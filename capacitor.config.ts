import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.segihovav.mylinks',
  appName: 'My Links',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
